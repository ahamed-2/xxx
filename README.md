# MyEyeSafeTelegramDemo

✅ Safe demo Android app that lets the user **manually** send a message to their own Telegram chat using their own bot token.

## What it does
- Stores Bot Token + Chat ID in SharedPreferences
- Sends user-entered message via Telegram `sendMessage`

## Important
- Use only with explicit user consent and your own accounts.
- This project intentionally does **not** include SMS/notification/file exfiltration features.
